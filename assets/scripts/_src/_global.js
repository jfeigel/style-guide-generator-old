+function($) {
  $(function() {
    // var appBar, mainContentContainer, $window;
    //
    // appBar = $('.app-bar');
    // mainContentContainer = $('.main-content-container');
    // $window = $(window);
    //
    // mainContentContainer.css('height', $window.height() - appBar.outerHeight(true));
  });
}(jQuery);
